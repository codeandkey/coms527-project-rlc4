/**
 * This file does not contain anything -- the main function will be
 * inserted automatically by gtest. Tests for components can be found along-
 * side their definitions in the source.
 */

#include <gtest/gtest.h>

TEST(TestSuiteTest, CanTest) {
    EXPECT_EQ(0, 0);
}